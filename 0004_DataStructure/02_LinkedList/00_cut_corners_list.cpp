#include <bits/stdc++.h>
using namespace std;

// 야매구현
const int MX = 1000005;
int dat[MX], prev[MX], next[MX];
int unused = -1;

int main() {
    fill(prev, prev + MX, -1);
    fill(next, next + MX, -1);
}